import{_ as n,c as a,a as s,o as t}from"./app-Dzh1Bu_Q.js";const o={};function i(r,e){return t(),a("div",null,e[0]||(e[0]=[s(`<h1 id="setup-thunder" tabindex="-1"><a class="header-anchor" href="#setup-thunder"><span>Setup Thunder</span></a></h1><h2 id="install-thunder" tabindex="-1"><a class="header-anchor" href="#install-thunder"><span>Install Thunder</span></a></h2><h3 id="system-requirements" tabindex="-1"><a class="header-anchor" href="#system-requirements"><span>System requirements</span></a></h3><p>You have to install <code>PHP</code>, <code>composer</code>, and <code>git</code> on your computer before you can install Thunder. The <code>composer</code> requires <code>git</code> command for proper functioning.</p><p>To install <code>PHP</code> please take a look at the official <a href="https://www.php.net/manual/install.php" target="_blank" rel="noopener noreferrer">Installation and Configuration</a> for <code>PHP</code>. On top of <code>PHP</code>, you need to install the required libraries. The Thunder installation requires at least all the libraries Drupal requires. Extensions used by Drupal core are defined in Core&#39;s composer.json file - see for example the <a href="https://git.drupalcode.org/project/drupal/blob/9.1.x/core/composer.json" target="_blank" rel="noopener noreferrer">file for Drupal 9.1.x</a>. Look at the &quot;require&quot; section and the keys starting with &quot;ext-&quot;.</p><p>The installation of <code>PHP</code> extensions can differ between operating systems, that&#39;s why you should check for detailed instructions on <code>PHP</code> <a href="https://www.php.net/manual/install.php" target="_blank" rel="noopener noreferrer">Installation and Configuration</a>.</p><p>To install <code>composer</code>, you can check the <code>composer</code> <a href="https://getcomposer.org/download" target="_blank" rel="noopener noreferrer">installation instructions</a> and for <code>git</code> you can find installation instructions <a href="https://git-scm.com/downloads" target="_blank" rel="noopener noreferrer">here</a>.</p><h3 id="project-setup" tabindex="-1"><a class="header-anchor" href="#project-setup"><span>Project setup</span></a></h3><p>To set up a new project, run this in your console to install Thunder from the command line:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">composer</span> create-project thunder/thunder-project thunder --no-interaction --no-install</span>
<span class="line"><span class="token builtin class-name">cd</span> thunder</span>
<span class="line"><span class="token function">composer</span> <span class="token function">install</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="quick-start" tabindex="-1"><a class="header-anchor" href="#quick-start"><span>Quick start</span></a></h3><p>To have a quick start run the following commands:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token builtin class-name">cd</span> docroot</span>
<span class="line">php core/scripts/drupal quick-start thunder</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>NOTE:</strong> This command is only useful to try Thunder locally, but not to run it in production nor to start a new project based on Thunder.</p><h3 id="beyond-quick-install" tabindex="-1"><a class="header-anchor" href="#beyond-quick-install"><span>Beyond quick install</span></a></h3><p>To develop your own website based on Thunder, you should have <code>mysql</code> installed on your computer.</p><p>Then you can install Thunder with:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush si thunder</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>As a next step, it&#39;s recommended to export your config files to a location outside the docroot. To do this change the location of <code>config_sync_directory</code> in <code>docroot/sites/default/settings.php</code>.</p><div class="language-php line-numbers-mode" data-highlighter="prismjs" data-ext="php"><pre><code class="language-php"><span class="line"><span class="token variable">$settings</span><span class="token punctuation">[</span><span class="token string single-quoted-string">&#39;config_sync_directory&#39;</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token string single-quoted-string">&#39;../config/sync&#39;</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>and export the config files:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush cex</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Now you should initialize a git repository for your project and commit all the files:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">git</span> init <span class="token builtin class-name">.</span> <span class="token parameter variable">-b</span> development</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> <span class="token builtin class-name">.</span></span>
<span class="line"><span class="token function">git</span> commit -m<span class="token string">&quot;Initial commit&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>From that point, you are ready to develop your new website based on Thunder.</p><p>For any further information on how to run and maintain your installation please refer to <a href="https://www.drupal.org/docs/user_guide/en/index.html" target="_blank" rel="noopener noreferrer">the Drupal User Guide</a>.</p><h2 id="update" tabindex="-1"><a class="header-anchor" href="#update"><span>Update</span></a></h2><p>Updating Thunder consists of three parts. First needs the code to be updated, and after that, it&#39;s required to update the database and then export the changes.</p><h3 id="code-update" tabindex="-1"><a class="header-anchor" href="#code-update"><span>Code update</span></a></h3><p>To update Thunder or any module to the newest version, constrained by the specified version in <code>composer.json</code>, use composer. The following command will check every dependency for a new version, downloads it, and updates it accordingly.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">composer</span> update</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="database-update" tabindex="-1"><a class="header-anchor" href="#database-update"><span>Database update</span></a></h3><h4 id="update-over-ui" tabindex="-1"><a class="header-anchor" href="#update-over-ui"><span>Update over UI</span></a></h4><p>After you have updated your code with <code>composer</code> command, you can go to your site page <code>/update.php</code> and follow instructions to update your site database.</p><h4 id="update-with-the-command-line" tabindex="-1"><a class="header-anchor" href="#update-with-the-command-line"><span>Update with the command line</span></a></h4><p>To update the database in the command line, you need to have <a href="http://docs.drush.org/en/master/install" target="_blank" rel="noopener noreferrer">drush</a> installed.</p><p>You can run <code>drush</code> command in the <code>docroot</code> folder of your site to update the database of your site like this:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush updb</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="configuration-export" tabindex="-1"><a class="header-anchor" href="#configuration-export"><span>Configuration export</span></a></h3><p>After the database was updated, it&#39;s necessary to export the changes to your configuration files.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush cex</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="extend" tabindex="-1"><a class="header-anchor" href="#extend"><span>Extend</span></a></h2><h3 id="find-extensions" tabindex="-1"><a class="header-anchor" href="#find-extensions"><span>Find extensions</span></a></h3><p>You can find extensions on <a href="https://www.drupal.org" target="_blank" rel="noopener noreferrer">Drupal.org</a>. On <a href="https://www.drupal.org/project/project_module?f%5B3%5D=drupal_core%3A7234" target="_blank" rel="noopener noreferrer">the following page</a>, you can search for <code>Modules</code> and on <a href="https://www.drupal.org/project/project_theme?f%5B2%5D=drupal_core%3A7234" target="_blank" rel="noopener noreferrer">this page</a> for <code>Themes</code>. You can find further information on extensions in <a href="https://www.drupal.org/docs/user_guide/en/extend-chapter.html" target="_blank" rel="noopener noreferrer">the Drupal User Guide - Extending and Customizing Your Site</a> .</p><p>If you know the name of the extension you are looking for, the fastest way is to search it using Google or any other search engine, adding <code>drupal</code> to the search. For example: <code>drupal webform</code></p><h3 id="add-extension" tabindex="-1"><a class="header-anchor" href="#add-extension"><span>Add extension</span></a></h3><p>Using &#39;composer&#39;, you can also manage the dependencies of your Thunder site and extensions.</p><p>To add an extension to your project, go to the root of your site (there should be a <code>composer.json</code> file) and add modules by typing</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">composer</span> require drupal/<span class="token punctuation">[</span>short name of the extension<span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>into the command line.</p><p>For example:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">composer</span> require drupal/webform</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="install-extension" tabindex="-1"><a class="header-anchor" href="#install-extension"><span>Install extension</span></a></h3><p>You can install extensions via the UI or the command line.</p><h4 id="install-over-ui" tabindex="-1"><a class="header-anchor" href="#install-over-ui"><span>Install over UI</span></a></h4><p>You can install modules by going to your site page <code>admin/modules</code>, or by clicking on <code>Extend</code> in the menu at the top. Here you can search for the module already added to your project by entering the name in the filter box at the top. To install a module, select the checkbox next to it, scroll to the bottom, and click <code>Install</code>. You might be warned that another module needs to be enabled because it is required for the module of your interest. By clicking on <code>continue</code>, Thunder will take care of that.</p><p>You can install themes by going to your site page <code>admin/appearance</code>, or by clicking on <code>Appearance</code> in the menu at the top. Here you can scroll to the theme you would like to install and click on <code>Install and set as default</code> to directly use the theme for your site.</p><h4 id="install-with-the-command-line" tabindex="-1"><a class="header-anchor" href="#install-with-the-command-line"><span>Install with the command line</span></a></h4><p>To install a theme or module at the command line, you need to have <a href="http://docs.drush.org/en/master/install" target="_blank" rel="noopener noreferrer">drush</a> installed.</p><p>You can run <code>drush</code> command in the <code>docroot</code> folder of your site to install a module like this:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush en <span class="token punctuation">[</span>module<span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>And to install a theme, you can run <code>drush</code> command like this:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush en <span class="token punctuation">[</span>theme<span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>To use a theme, you still have to navigate to <code>Appearance</code> (admin/appearance) and set it as default.</p><h3 id="uninstall-extension" tabindex="-1"><a class="header-anchor" href="#uninstall-extension"><span>Uninstall extension</span></a></h3><p>To uninstall an extension, you have to uninstall it first and then remove the code. You can uninstall extensions via the UI or the command line, but to remove the code from your project, you have to use <code>composer</code>.</p><h4 id="uninstall-in-ui" tabindex="-1"><a class="header-anchor" href="#uninstall-in-ui"><span>Uninstall in UI</span></a></h4><p>You can uninstall modules by going to your site page <code>admin/modules</code>, or by clicking on <code>Extend</code> in the menu at the top and then by clicking on <code>Uninstall</code> tab. Here you can search for the module by entering the name in the filter box at the top. To uninstall a module, select the checkbox next to it, scroll to the bottom, and click <code>Uninstall</code>. You might be warned that another module needs to be uninstalled because it depends on the module that you want to remove. By clicking on <code>continue</code>, Thunder will take care of that.</p><p>You can uninstall themes by going to your site page <code>admin/appearance</code>, or by clicking on <code>Appearance</code> in the menu at the top. Here you can scroll to the theme you would like to uninstall. If your site is using that theme as default, you have to select another default theme before you can uninstall it. When your site is not using that theme as default, you can click <code>Uninstall</code> next to it to uninstall it.</p><h4 id="uninstall-with-command-line" tabindex="-1"><a class="header-anchor" href="#uninstall-with-command-line"><span>Uninstall with command line</span></a></h4><p>To uninstall a theme or module at the command line, you need to have <a href="http://docs.drush.org/en/master/install" target="_blank" rel="noopener noreferrer">drush</a> installed.</p><p>After that, you can run <code>drush</code> command in the <code>docroot</code> folder of your site to uninstall a module like this:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush pm:uninstall <span class="token punctuation">[</span>module<span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>To uninstall a theme, you still have to select another theme as default. It&#39;s explained in <code>Uninstall in UI</code> part.</p><p>And then you to uninstall a theme, you can run <code>drush</code> command like this:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">drush theme:uninstall <span class="token punctuation">[</span>theme<span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="remove-extension" tabindex="-1"><a class="header-anchor" href="#remove-extension"><span>Remove extension</span></a></h3><p>After you have uninstalled an extension from your site, you can also remove the code from your project.</p><p>If you want to remove a module, that was provided by Thunder, you will have to add it to your composer.json file in the replace block. Modules, that you added yourself by the above commands, do not have to be placed there.</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;replace&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;drupal/diff&quot;</span><span class="token operator">:</span> <span class="token string">&quot;*&quot;</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For more information on using the composer replace check the official <a href="https://getcomposer.org/doc/04-schema.md#replace" target="_blank" rel="noopener noreferrer">composer documentation</a></p><p>To remove code you can execute a command like this:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">composer</span> remove drupal/<span class="token punctuation">[</span>short name of the extension<span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div>`,83)]))}const d=n(o,[["render",i]]),c=JSON.parse('{"path":"/developer-guide/setup.html","title":"Setup Thunder","lang":"en-US","frontmatter":{},"git":{"updatedTime":1766052996000,"contributors":[{"name":"Daniel Bosen","username":"","email":"daniel.bosen@burda.com","commits":1}],"changelog":[{"hash":"ff82b5e334b60cf225703a1c19508730634995b5","time":1766052996000,"email":"daniel.bosen@burda.com","author":"Daniel Bosen","message":"#3563825 Release Thunder 8.2.7"}]},"filePathRelative":"developer-guide/setup.md"}');export{d as comp,c as data};
